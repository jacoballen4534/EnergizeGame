package model;

import java.awt.*;

//Pixel reader and pixel writer to create the image itself

public class Minimap {

    private Map map;

    public Minimap(Map map) {
        this.map = map;
    }

    public void tick(){
        //Draw map of current room
        //Update position of player on map
    }
}
